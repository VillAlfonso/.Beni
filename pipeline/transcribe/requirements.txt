# Python 3.11. The RTX 5060 Ti is Blackwell (sm_120) → torch needs cu128+, NOT cu124:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# then:
#   pip install -r requirements.txt
# ffmpeg lives at C:\ffmpeg. The .venv here was set up 2026-07-18.
#
# pyannote is OPTIONAL (better diarization). Without it the pipeline uses ECAPA
# greedy clustering — no token needed. To enable pyannote: pip install
# pyannote.audio, accept terms at hf.co/pyannote/speaker-diarization-3.1,
# set HF_TOKEN.
faster-whisper>=1.0.3
speechbrain>=1.0.0
numpy
soundfile
